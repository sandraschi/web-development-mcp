[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "web-development-mcp"
version = "1.0.0"
description = "Universal web development operations MCP server supporting modern frontend frameworks and tooling"
authors = [
    {name = "Sandra", email = "sandra@example.com"}
]
dependencies = [
    "fastmcp>=2.13.0,<3.0.0",
    "jinja2>=3.0.0",
    "requests>=2.28.0",
    "semver>=3.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "pytest-asyncio>=0.21.0",
    "black>=23.0.0",
    "isort>=5.12.0",
    "mypy>=1.5.0",
]

[project.scripts]
web-development-mcp = "web_development_mcp.__main__:main"

[tool.black]
line-length = 100
target-version = ['py39']

[tool.isort]
profile = "black"
line_length = 100

[tool.hatch.build.targets.wheel]
packages = ["src/web_development_mcp"]

[tool.ruff]
line-length = 100
target-version = "py39"

[tool.ruff.lint]
ignore = ["E402"]  # Allow imports not at top of file (needed for FastMCP pattern)

[tool.mypy]
python_version = "3.9"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
