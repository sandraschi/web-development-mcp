import { useState } from 'react';
import { motion } from 'framer-motion';
import { Activity, Github } from 'lucide-react';

function App() {
  const [count, setCount] = useState(0);

  return (
    <motion.div
      initial={{ opacity: 0, y: 8 }}
      animate={{ opacity: 1, y: 0 }}
      className="min-h-screen bg-zinc-950 text-zinc-100 flex flex-col items-center justify-center p-8"
    >
      <Activity className="w-12 h-12 text-blue-400 mb-4" />
      <h1 className="text-2xl font-semibold mb-2">{project_name}</h1>
      <p className="text-zinc-400 mb-6">Scaffolded with web-development-mcp</p>

      <div className="flex items-center gap-4 mb-8">
        <button
          onClick={() => setCount((c) => c + 1)}
          className="px-4 py-2 bg-blue-600 hover:bg-blue-500 text-white rounded-lg transition-colors"
        >
          count is {count}
        </button>
      </div>

      <a
        href="https://github.com/{{project_name}}"
        target="_blank"
        rel="noopener noreferrer"
        className="flex items-center gap-2 text-sm text-zinc-500 hover:text-zinc-300"
      >
        <Github size={14} />
        View on GitHub
      </a>
    </motion.div>
  );
}

export default App;
